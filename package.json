{
  "name": "eclipse-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "clean": "node -e \"const fs = require('fs'); const path = require('path'); const dir = '.next'; if (fs.existsSync(dir)) { fs.rmSync(dir, { recursive: true, force: true }); console.log('Cleaned .next directory'); } else { console.log('.next directory does not exist'); }\"",
    "clean:all": "node -e \"const fs = require('fs'); const dirs = ['.next', 'node_modules/.cache']; dirs.forEach(d => { if (fs.existsSync(d)) { fs.rmSync(d, { recursive: true, force: true }); console.log(`Cleaned ${d}`); } });\"",
    "dev:clean": "rimraf .next .turbo && next dev"
  },
  "dependencies": {
    "firebase": "^11.7.3",
    "firebase-admin": "^13.6.0",
    "framer-motion": "^12.12.1",
    "next": "15.3.8",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.5.0",
    "twilio": "^5.7.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.4",
    "rimraf": "^6.0.1",
    "tailwindcss": "^4.1.8"
  }
}
